# Only needed when the backend is hosted separately from this frontend
# (e.g. this frontend on Vercel, the API on Render) - the backend's base
# URL, with no trailing slash. Leave unset for a combined/same-origin
# deployment (dev, or a single server serving both) - '/api' is used as-is.
VITE_API_URL=
